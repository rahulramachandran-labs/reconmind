# Verify it yourself

The written companion to the [/verify page](https://reconmind-labs.vercel.app/verify) on the live app. For each problem planted in the synthetic data: what the generator planted, what the agents found, the test that proves they find it, and what the template and the model each wrote about it. Then how to ask your own question, and where the evaluation scores come from.

The findings and write-ups are from the captured run in [`docs/evidence/`](evidence/README.md) (2026-09-23). Links to code are pinned to commit `1bf31ac`. This file is regenerated by [`scripts/update_docs_from_evidence.py`](../scripts/update_docs_from_evidence.py).

## Planted, found, proven

### Key drift

| | |
|---|---|
| **Planted** | `LOC-0517` reports under its canonical `OUT-1017` and under `OUT-1071`, which isn't in `outlet_location_map`: **251** of 8,373 deduplicated rows (3.0%). Source: [`DATA_DICTIONARY.md`](../data/DATA_DICTIONARY.md), [`expected_anomalies.json`](../data/sample/expected_anomalies.json) |
| **Expected** | `key_drift` from the Reconciliation agent, S2 |
| **Found** | S2 *LOC-0517 also reporting as OUT-1071 (3.0% of rows)*, 251 records, by the reconciliation agent; published |
| **Proven by** | [`test_each_anomaly_is_caught_by_the_right_agent[key_drift]`](https://github.com/rahulramachandran-labs/reconmind/blob/1bf31accf5405378d0664355fed17ce8d0a0f096/tests/chaos/test_injected_anomalies.py#L60-L72): plants only this anomaly into a fresh database and fails unless the right agent reports it at the right severity. Runs in CI on every push. |
| **Template's root cause** | Whole baskets (158) from 4 submitter(s) carry OUT-1071, so the id is set at the register or export profile rather than corrupted row by row; OUT-1071 looks like a transposition of OUT-1017. |
| **Model's root cause** | The source system that uploads transaction files for location LOC-0517 emitted an incorrect outlet_id (OUT-1071) in the header for a subset of rows. Because OUT-1071 is not present in the authoritative outlet_location_map (canonical OUT-1017), the transactions were ingested with a drifted key, causing key‑drift between location_id and outlet_id for 251 rows (0.03% of the window). This is a classic source‑header mismatch rather than a processing bug, as the raw layer is immutable and the drift was only detected by the deterministic key‑drift detection query. *(groq/openai/gpt-oss-120b, 8,842 ms, 1,762 tokens, $0)* |

### Duplicate submission

| | |
|---|---|
| **Planted** | `S1002_20260612_1120_ECOMM.txt` repeats every key from `S1002_20260612_0304_ECOMM.txt`: **88** superseded rows, **3** with a corrected `qty` and `amount`, landed after that night's DAG run. Source: [`DATA_DICTIONARY.md`](../data/DATA_DICTIONARY.md), [`expected_anomalies.json`](../data/sample/expected_anomalies.json) |
| **Expected** | `duplicate_submission` from the Reconciliation agent, S2 |
| **Found** | S2 *Resent file S1002_20260612_1120_ECOMM.txt supersedes 88 rows*, 88 records, by the reconciliation agent; published |
| **Proven by** | [`test_each_anomaly_is_caught_by_the_right_agent[duplicate_submission]`](https://github.com/rahulramachandran-labs/reconmind/blob/1bf31accf5405378d0664355fed17ce8d0a0f096/tests/chaos/test_injected_anomalies.py#L60-L72): plants only this anomaly into a fresh database and fails unless the right agent reports it at the right severity. Runs in CI on every push. |
| **Template's root cause** | A deliberate correction: 3 rows changed qty or amount, so the later file is the one to trust. It landed after dedupe_transactions had run, so nothing removed the older copies before publish. |
| **Model's root cause** | The submitter S1002 re‑ran the export for business date 2026‑06‑12 and uploaded a second file (S1002_20260612_1120_ECOMM.txt) after the original file (S1002_20260612_0304_ECOMM.txt) had already been processed by the `retail_txn_daily` DAG run that completed at 2026‑06‑13T05:36:38Z. Because the DAG’s dedupe step runs only once per run, the resend landed after deduplication and therefore the 88 duplicate keys were not removed, causing the newer file to supersede the older rows under the latest‑file‑wins rule. *(groq/openai/gpt-oss-120b, 1,400 ms, 1,655 tokens, $0)* |

### Schema drift

| | |
|---|---|
| **Planted** | `S1003_20260616_0216_MOBILE.txt` renamed `channel_basket_id` to `basket_ref`: **82** rows land with a null dedup-key column and `validate_schema` fails with a `ContractViolation`. Source: [`DATA_DICTIONARY.md`](../data/DATA_DICTIONARY.md), [`expected_anomalies.json`](../data/sample/expected_anomalies.json) |
| **Expected** | `schema_drift` from the Data-Quality agent, S1 |
| **Found** | S1 *S1003_20260616_0216_MOBILE.txt renamed channel_basket_id to basket_ref*, 82 records, by the data-quality agent; held for review |
| **Proven by** | [`test_each_anomaly_is_caught_by_the_right_agent[schema_drift]`](https://github.com/rahulramachandran-labs/reconmind/blob/1bf31accf5405378d0664355fed17ce8d0a0f096/tests/chaos/test_injected_anomalies.py#L60-L72): plants only this anomaly into a fresh database and fails unless the right agent reports it at the right severity. Runs in CI on every push. |
| **Template's root cause** | The submitter's export renamed channel_basket_id to basket_ref: one contracted column is missing and one similarly named column appeared in the same batch. The loader maps by name, so the rows landed with the column null. This matches the pattern in INC-0438. |
| **Model's root cause** | The mobile export process renamed the dedup‑key column `channel_basket_id` to `basket_ref`. The rename was not communicated to the data ingestion team, so the `validate_schema` task saw a missing required column and an unexpected column, causing the schema‑drift violation and null values for the original column in the raw table. *(groq/openai/gpt-oss-120b, 1,840 ms, 1,721 tokens, $0)* |

### Volume anomaly

| | |
|---|---|
| **Planted** | `S1001_20260618_0638_POSFEED.txt` has **110** rows against a trailing 7-day average of **182.6** (40% below) and landed **161** minutes after the submitter SLA. Source: [`DATA_DICTIONARY.md`](../data/DATA_DICTIONARY.md), [`expected_anomalies.json`](../data/sample/expected_anomalies.json) |
| **Expected** | `volume_anomaly` from the Data-Quality agent, S2 |
| **Found** | S2 *S1001 2026-06-18: 110 rows, 40% below its 7-day average*, 73 records, by the data-quality agent; published |
| **Proven by** | [`test_each_anomaly_is_caught_by_the_right_agent[volume_anomaly]`](https://github.com/rahulramachandran-labs/reconmind/blob/1bf31accf5405378d0664355fed17ce8d0a0f096/tests/chaos/test_injected_anomalies.py#L60-L72): plants only this anomaly into a fresh database and fails unless the right agent reports it at the right severity. Runs in CI on every push. |
| **Template's root cause** | A late and light file together is the signature of a truncated upstream export: the transfer shipped whatever was there once its retry window ran out. This matches the pattern in INC-0451. |
| **Model's root cause** | The POSFEED file for submitter S1001 on 2026-06-18 was truncated during transfer or ingestion, resulting in only 110 rows (40% below the 7‑day trailing average of 182.6). The truncation also caused the ingestion sensor to run for the full 5400 s timeout, delaying file landing by 161 minutes past the 04:00 SLA. *(groq/openai/gpt-oss-120b, 1,355 ms, 1,808 tokens, $0)* |

A clean pipeline raises nothing ([test](https://github.com/rahulramachandran-labs/reconmind/blob/1bf31accf5405378d0664355fed17ce8d0a0f096/tests/chaos/test_injected_anomalies.py#L75-L82)), and a full investigation fits a 30-second budget ([test](https://github.com/rahulramachandran-labs/reconmind/blob/1bf31accf5405378d0664355fed17ce8d0a0f096/tests/chaos/test_injected_anomalies.py#L85-L95)).

## Ask your own question

The [/verify page](https://reconmind-labs.vercel.app/verify) and **Ask ReconMind** send any question through the live agent graph and link its trace. From a terminal:

```bash
curl -N https://reconmind-labs-api.onrender.com/chat/stream \
  -H 'content-type: application/json' \
  -d '{"question": "Which submitter files landed late last week, and why?"}'
```

The `plan` event shows how the Planner routed it, `answer` or `summary` names the model that wrote the reply, and `run` gives the id to open under **Traces**. `GET /model` says which model is answering right now.

## Evaluation scores

The latest row for each retriever, answerer and judge in [`evals/history.csv`](../evals/history.csv):

| Retriever | Answers written by | Judged by | Faithfulness | Answer relevancy | Context precision | Context recall | Gate |
|---|---|---|---|---|---|---|---|
| dense | `extractive` | `offline` | 0.920 | 0.863 | 0.661 | 0.844 | fail |
| hybrid | `extractive` | `offline` | 0.911 | 0.862 | 0.830 | 0.922 | pass |
| hybrid | `ollama/qwen2.5:1.5b` | `offline` | 0.427 | 0.624 | 0.830 | 0.922 | fail |
| hybrid | `groq/openai/gpt-oss-120b` | `offline` | 0.566 | 0.943 | 0.830 | 0.922 | fail |
| hybrid | `groq/openai/gpt-oss-120b` | `llm:groq/qwen/qwen3.8-27b` | 0.935 | 0.720 | 0.830 | 0.922 | fail |

How each judge works, and why the gate runs on extractive answers, is in [EVALUATION.md](EVALUATION.md).

## Reproduce it

```bash
make bootstrap && make dev                          # seeded stack on :8000 and :3000
uv run pytest -m chaos -v                           # each planted anomaly, one at a time
uv run python scripts/capture_readme_evidence.py    # the scenarios behind docs/evidence/
make eval                                           # RAGAS on the golden set
```
