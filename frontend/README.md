# ReconMind web

Next.js (App Router) + shadcn/ui front end for the ReconMind API.

```bash
npm ci
NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev
```

`NEXT_PUBLIC_API_URL` points at the FastAPI backend. On Vercel it is set per environment.
